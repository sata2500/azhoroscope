/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Next.js 16'da i18n App Router için desteklenmiyor
  // Dil yönetimi için next-intl veya middleware kullanılacak
};

module.exports = nextConfig;
